// === NAME - DESCRIPTION: Central Limit Theorem - select all true statements about when the CLT applies and what it says (and does not say) about sample means and proportions ===
// === SET QUESTION TYPE TO: multans ===

// === COMMON CONTROL (paste into Common Control) ===

$questions = array(
  "The Central Limit Theorem says that the <i>population</i> becomes approximately normal when the sample size is large.",
  "The Central Limit Theorem says that the <i>sampling distribution of</i> `bar(x)` is approximately normal for large `n`, regardless of the population shape.",
  "The Central Limit Theorem requires the population to be normally distributed.",
  "For the sampling distribution of `\hat{p}` to be approximately normal, we need both `n cdot p >= 10` and `n cdot (1-p) >= 10`.",
  "As `n` increases, the standard error of `bar(x)` decreases.",
  "If the population is approximately normal, the sampling distribution of `bar(x)` is also approximately normal even when `n` is small.",
  "The Central Limit Theorem guarantees that any single observation drawn from a non-normal population is approximately normal when `n` is large.",
  "A rule of thumb is that `n >= 30` is large enough for the sampling distribution of `bar(x)` to be approximately normal when the population is not strongly skewed."
)

// Correct = TRUE statements: indices 1, 3, 4, 5, 7
$answers = "1,3,4,5,7"

// === QUESTION TEXT ===

<div style="font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Arial,sans-serif; font-size:16px; line-height:1.6; color:#21242c; max-width:688px;">
  <div style="background:#fff; border:1px solid #e5e7eb; border-radius:12px; padding:20px; margin:10px 0; box-shadow:0 4px 6px -1px rgba(0,0,0,0.07),0 2px 4px -2px rgba(0,0,0,0.04);">
    <p style="margin:0;">Select <b>all</b> of the following statements that are <b>true</b> about the Central Limit Theorem and the conditions for using normal sampling distributions.</p>
  </div>
</div>
