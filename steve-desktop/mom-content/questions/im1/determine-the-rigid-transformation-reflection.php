// === NAME - DESCRIPTION: Determine the rigid transformation (reflection) ===
// === SET QUESTION TYPE TO: multipart ===

// === COMMON CONTROL ===

$anstypes = "choices"

// Define the transformation options
$choices = array("Rotation", "Reflection", "Translation", "None");

// Assign the correct transformation for each pair of shapes
$answer[0] = 1  // For part a

// Show the correct answer after submission
$showanswer[0] = "Reflection";

// === QUESTION TEXT ===

<p>Decide which transformation was used on the pair of shapes below. Some may have undergone more than one transformation, but try to name a single transformation, if possible.</p>
$shape

a. $answerbox[0]
